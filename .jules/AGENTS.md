# Operational Memory

Where key concerns live in this repository:

- `fdroid/repo/` -> f-droid repository data
- `src/scripts/` -> automation scripts

## Error Handling

- **Centralized Error Reporting:** The project must route all unexpected errors through a single, centralized error reporting function. No script or file should fail silently or rely solely on standard output.
